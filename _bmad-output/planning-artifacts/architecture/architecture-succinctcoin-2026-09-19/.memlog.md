---
scope: SuccinctCoin desktop wallet (whole system)
purpose: BMad grounding: consistency contract for bmad-build/epics agents over the existing codebase
altitude: feature
updated: 2026-09-20T11:58
---

- (direction) Coaching path; spine-only deliverable; brownfield (ratify existing code + 12 OpenSpec specs); altitude=feature; run seeded from spine-template.md
- (constraint) Codebase facts from scan: api.js is the composition root owning singletons (blockchain, transactionPool, wallet, account, pubsub, transactionMiner); renderer is stateless UI over preload IPC + HTTP; core (src/main/blockchain, util/crypto) is Electron-free; two config modules exist (src/config.js, src/main/config.js) with GENESIS_DATA defined in src/main/config.js matching canonical spec values; fee auto = Big(amount).div(1000) in api.js; syncWithRootState fires fetch(root /api/blocks + pool) -> replaceChain/setMap with NO precedence vs peer replaceChain
- (question) OPEN: state-mutation ownership — who may mutate the shared singletons, and what is the rule for root-sync vs peer-sync (code has a race; spec defines root-first precedence)
- (decision) AD candidate [ADOPTED by user]: blockchain core (src/main/blockchain + util/crypto) is a headless library, decoupled from Electron, the app shell, P2P, and HTTP; it never imports them. Binds: core units. Prevents: core drifting into app coupling, making it untestable/unreusable.
- (decision) AD candidate [ADOPTED by user]: symmetric network - all nodes are equal; no privileged/root authority node. Consensus rule: longer valid chain wins (already implemented in Blockchain.replaceChain). Binds: p2p, blockchain-core, api. Prevents: reintroducing a root-authority sync path.
- (decision) AD candidate [ADOPTED by user]: equal mining eligibility - any running node may mine; no privilege purchasable via hardware or staked balance; block reward goes to the miner of the winning block. Binds: transaction-miner, transaction-system, configuration. Prevents: stake-weighted or hardware-weighted mining re-entering the design.
- (question) OPEN: Sybil resistance - multi-wallet cheating is a stated goal but NO mechanism is settled. Candidates: off-chain proof-of-personhood, economic bond/slashing (conflicts with no-stake-privilege), or accept-and-cap. Boundary invariant is settled (no purchasable privilege); mechanism is deferred. Need user input on which candidate family to keep open.
- (event) CONFLICT FOUND: user's all-nodes-equal contradicts (a) code: src/config.js ROOT_NODE_ADDRESS = localhost:port (root is self in dev), api.js syncWithRootState() fetches root /api/blocks + pool and calls replaceChain/setMap at startup, and (b) spec: p2p-networking 'Chain-sync precedence' ADDED in reconcile-spec-conflicts (archived 2026-09-19) mandates root-first init on empty chain. Must be resolved: keep root as dev-only bootstrap, or delete the concept (pure peer sync; spec needs a follow-up change).
- (decision) AD root-bootstrap [ADOPTED by user, keep-for-now]: the root node is retained ONLY as bootstrap/initial seed for an empty-chain node (api.syncWithRootState at startup). It is NOT a standing authority - a longer valid chain from any peer still wins (Blockchain.replaceChain). Binds: p2p-networking, api. Prevents: treating root state as canonical over peer state. Spec requirement 'Chain-sync precedence' (p2p-networking) stays as written for now; demoting/removing root is a later improvement request.
- (direction) Sybil / multi-wallet prevention is OUT OF SCOPE for this spine - user will address it in a separate later request. Recorded in Deferred with the locked boundary invariant: no privilege is purchasable (rules out bond/slashing designs that favor large stake). No sybil mechanism may be designed in this run.
- (event) Closed OPEN question 'sybil resistance mechanism': deferred to separate future request per user; boundary invariant (no purchasable privilege) already captured as ADOPTED decision. Spine's Deferred section must list it with this reason-it-can-wait.
- (decision) AD miner-selection [ADOPTED by user, option B per-block lottery]: the miner of block h+1 is determined by a deterministic pure function of (block h hash, candidate node public key), computable independently by every peer - no coordination, no shared state, no trust. Hardware speed and staked balance are NOT inputs to selection; equal odds per running node. Binds: transaction-miner, blockchain-core, p2p-networking. Prevents: reintroducing first-come race (hardware privilege) or stake-weighted selection. Consequences: (1) mining becomes AUTONOMOUS - each running node checks the lottery against its current head on an interval; the HTTP mine endpoint degrades to a dev convenience. (2) the lottery function is pure and verifiable by any peer, so it belongs in the decoupled core (or a core-adjacent pure module), never in app/P2P layers.
- (question) OPEN (spec detail, deferred - not a spine blocker): exact lottery mapping - fixed-odds threshold on hash(pubkey, prevHash) vs argmin-over-known-peers. Both satisfy the AD; the variant is decided in the OpenSpec change that implements miner selection. VALIDATION_RATE (1000ms, currently unused) is the natural check interval.
- (decision) AD discovery-bootstrap [ADOPTED by user]: a small set of well-known discv5 bootstrap ENRs (plus optional relay endpoints) ships in app-level config to seed peer discovery for zero-peer new nodes. Bootstrap-only - it seeds DISCOVERY, not chain state; running a bootstrap node confers no mining or state authority; list rotation is a later improvement (ENRs/RELAY currently empty TODOs in src/config.js). Binds: p2p-networking, configuration. Prevents: treating discovery seeds as trusted state sources.
- (decision) AD composition-root [RATIFIED by user, matches existing code]: api.js is the single composition root - the only module that instantiates the blockchain, transaction pool, wallet, pubsub, and miner singletons; all other units receive dependencies via constructor injection. Binds: api, all app units. Prevents: scattered singleton construction and hidden coupling between app units.
- (decision) AD config-ownership [RATIFIED by user, fixes two-config-modules smell]: one owner per value. Chain-semantic values (GENESIS_DATA, rewards, stake rules, validation rate) belong to the core config (src/main/config.js, consumed by core). Network/app values (ports, channels, discv5 bootstrap ENRs, relay endpoints) belong to app-level config (src/config.js). The renderer must not import config files directly - it receives UI values (API port) via the preload bridge. Mechanical move is a later OpenSpec change; the spine records the rule. Binds: configuration capability, renderer-ui, p2p-networking. Prevents: two modules owning the same value (current dual-owner state).
- (decision) AD error-flow [RATIFIED by user, matches existing pattern]: core units throw on invalid state; the app layer (api.js / IPC handlers) catches and maps to HTTP status codes or IPC responses. Core never logs to stderr or console on its own - it reports by throwing. Binds: blockchain-core, api, renderer-ui (IPC responses). Prevents: core leaking I/O side effects (breaks headless purity) and inconsistent error surfaces between HTTP and IPC.
- (event) Coaching elicitation COMPLETE. All load-bearing calls settled: paradigm (decoupled headless core), network (symmetric, root=bootstrap-only, discovery ENRs=bootstrap-only), mining (lottery selection, autonomous interval, no purchasable privilege), stake (neutral lock, ratified), sybil (deferred, boundary locked), composition root / config split / error flow (ratified). Open questions remaining are spec-detail only (exact lottery mapping; DISCV5 ENR list contents). Moving to distill phase.
- (event) Closed OPEN question 'state-mutation ownership': settled by the AD combination - AD-7 (composition root owns the singletons), AD-1 (core is headless, mutated only through its own methods), AD-2/AD-5 (chain state only via replaceChain validity+length rules), plus the standing convention that transactions are created only via Wallet.createTransaction and are immutable after signing. No new AD needed. Root/peer sync race: the RULE is settled (root only seeds an empty chain; longer valid chain always wins); the code not yet enforcing the empty-chain-only precedence is a Deferred follow-up, not a spine gap. Distill phase: writing spine from memlog now.
- (decision) AD discovery-bootstrap [ADOPTED by user via confirm]: discv5 bootstrap ENRs are retained as a small well-known seed set (config DISCV5_BOOTSTRAP_ENRs, currently empty TODO) ONLY for initial discovery - anyone can run one, it only seeds peer discovery, never chain state; rotating the list is a later improvement. Same bootstrap-not-authority posture as the root node. Binds: p2p-networking, configuration. Prevents: privileged discovery node. AD two-config-splits [ADOPTED via confirm]: one owner per value - chain-semantic values (genesis, rewards, stake rules) live in src/main/config.js / core; network/app values (ports, channels, discovery) live at app level. Renderer no longer imports config files; the single UI value it needs (API port) is passed via the preload bridge. Mechanical move is a later OpenSpec change; the rule is recorded now. Binds: api, renderer, configuration. AD composition-root [ADOPTED via confirm]: api.js stays the sole composition root - only place that instantiates the blockchain, pool, wallet, pubsub, miner singletons; everything else gets deps via constructor. AD error-flow [ADOPTED via confirm]: core throws, app layer catches and maps to HTTP/IPC status; core never logs to stderr on its own.
- (decision) AD discovery-bootstrap [ADOPTED by user]: a small set of well-known bootstrap ENRs shipped in app config (src/config.js DISCV5_BOOTSTRAP_ENRs, currently empty with VPS TODO) seeds discv5 for a zero-peer new node. Seeds only - no chain-state authority; same bootstrap-not-authority posture as the root. RELAY_ENDPOINTS remain empty until real VPS relays exist. Binds: p2p-networking, configuration. Prevents: treating bootstrap/relay nodes as trusted authorities.
- (decision) AD composition-root [ADOPTED by user, brownfield ratification]: api.js is the single composition root - the only place that instantiates blockchain, transactionPool, wallet, account, pubsub, transactionMiner. All other units receive dependencies via constructor injection. Binds: api, all core and app units. Prevents: scattered singleton creation and hidden coupling.
- (decision) AD config-ownership [ADOPTED by user]: one owner per value. Chain-semantic values (GENESIS_DATA, rewards, stake rules, VALIDATION_RATE) belong to core config src/main/config.js. Network values (ports, CHANNELS, discovery ENRs, relays) belong to app config src/config.js. The renderer must not import config files directly - it receives UI values via the preload bridge. The mechanical move of renderer imports is a follow-up OpenSpec change; the spine records the rule. Binds: configuration, renderer-ui, api, p2p-networking. Prevents: two-config-module drift (two owners of one value).
- (decision) AD error-flow [ADOPTED by user, target state - current code has core logging to stderr in Blockchain.addBlock]: core units THROW on invalid state; the app layer (api.js routes, IPC handlers) catches and maps to HTTP status codes or IPC responses. Core never logs to stderr by itself. Binds: blockchain-core, util/crypto, api, electron-app. Prevents: error handling scattered across layers and silent core failures.
- (event) Distill phase: writing spine from memlog now. All elicitation settled; open items are spec-detail only (lottery mapping, ENR contents) and deferred (sybil, root demotion, renderer config move, lottery implementation, root/peer precedence enforcement).
- (event) REVIEWER GATE results: rubric walker PASS-WITH-FINDINGS (2 HIGH: F1 AD-4 target-state unmarked vs transaction-miner/api specs; F2 AD-5 vs Chain-sync-precedence spec scope; 3 MED; 4 LOW - full text in reviews/review-rubric.md). Version lens PASS (all 15 stack pins verified vs package.json+node_modules; Node v24.14.0; secp256k1 3.1.0 transitive via discv5, externalized+mocked confirmed; all code-behavior claims verified; info: 4 core logging leaks not 1; Electron 42 is 2 majors behind 44.4.3). Adversary lens FAIL: A-1 CRITICAL no state-transition function - tx.validate() reads local on-disk balances never written by any production path, so validity is node-relative and every user tx fails on all nodes (chain stuck at height 3); A-2 CRITICAL lottery variant+encoding fully deferred and variants not equivalent (argmin-over-known-peers is peer-set-dependent, violates peer-invariance); A-3 HIGH intra-block tx order not canonical (inconsistent timestamp comparator on ties) and it is a hash input; A-4 HIGH block-hash field set unpinned (algorithm only); A-5 HIGH root POOL sync ungated setMap full replace (chain half defended by AD-2, pool half not); A-6..A-9 MED/LOW tie-break, interval ownership, error envelope, preload shape. Full text in reviews/review-adversary.md. Empirical probes: Crypto.hash sort kills scalar arg-order (defended) but NOT hex-vs-bytes encoding; Big.js vs JSON round-trip stable by accident.
- (decision) GATE FIXES applied to spine (finalize, applied by agent - all are consequences of ADOPTED ADs, no new product calls): NEW AD-10 deterministic state-transition (validity pure function of chain contents; account state chain-derived, never local-disk input; [TARGET-STATE] - current code reads disk balances that are never written). NEW AD-11 lottery family pinned: fixed-odds threshold on Crypto.hash(prevBlockHash hex, publicKey hex); argmin-over-known-peers EXCLUDED (peer-set-dependent, violates AD-4 peer-invariance); input canonical form fixed (both hex strings); only the threshold probability p deferred (single tunable, core config). NEW AD-12 canonical block contents: hash field set exactly (height,uuid,timestamp,miner,lastHash,data) + data total order (timestamp ASC, uuid ASC); field-set/order change = hard fork. NEW AD-13 pool sync empty-gated, additive merge, never full replace [TARGET-STATE - current syncWithRootState setMap is a known violation]. NEW AD-14 equal-length fork: build on lower hash at divergence point; incoming equal-length rejected; convergence by strictly-longer extension. Conventions added: state transition, canonical block contents, lottery input form, pool sync, mining re-check interval (app-semantic, distinct from VALIDATION_RATE per AD-8 amendment), error envelope {type:'error',code,message} + status table, preload bridge (getApiPort()). AD-4 marked [TARGET-STATE] + peer-invariance clause; AD-5 pool clause + Chain-sync-precedence spec rewrite recorded; AD-8 renderer clause + AD-9 marked [TARGET-STATE]; core I/O bullet rephrased (cross-store account reads ratified); envelope expanded (key custody = plaintext ratified, encrypted backup deferred; NAT = relay+DCUtR+UPnP; dev = electron-is-dev, .test/ via JEST_WORKER_ID only); capability map + AD binds reconciled (AD-3 +account-management; AD-5 +blockchain-core). Deferred updated: state-transition launch-prerequisite (CRITICAL), ungated setMap fix, autonomous-mining spec rewrites, recursive key sort in Crypto.hash (crypto-utils spec gap), key custody encrypted backup (wallet spec target), electron-app dev-storage spec clause.
- (event) Spine finalized: lint_spine 0 findings (pre and post gate fixes); all gate findings triaged and applied (see reviews/ for the three full reviews); frontmatter status: final. Deliverable: ARCHITECTURE-SPINE.md, 14 ADs (AD-1..AD-9 ratified/adopted, AD-10..AD-14 added by the gate), 9 conventions, verified stack (15 pins), capability map (12), 12 deferred items with reasons. Run complete.
