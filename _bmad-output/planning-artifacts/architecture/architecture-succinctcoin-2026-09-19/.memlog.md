---
scope: SuccinctCoin desktop wallet (whole system)
purpose: BMad grounding: consistency contract for bmad-build/epics agents over the existing codebase
altitude: feature
updated: 2026-09-19T15:29
---

- (direction) Coaching path; spine-only deliverable; brownfield (ratify existing code + 12 OpenSpec specs); altitude=feature; run seeded from spine-template.md
- (constraint) Codebase facts from scan: api.js is the composition root owning singletons (blockchain, transactionPool, wallet, account, pubsub, transactionMiner); renderer is stateless UI over preload IPC + HTTP; core (src/main/blockchain, util/crypto) is Electron-free; two config modules exist (src/config.js, src/main/config.js) with GENESIS_DATA defined in src/main/config.js matching canonical spec values; fee auto = Big(amount).div(1000) in api.js; syncWithRootState fires fetch(root /api/blocks + pool) -> replaceChain/setMap with NO precedence vs peer replaceChain
- (question) OPEN: state-mutation ownership — who may mutate the shared singletons, and what is the rule for root-sync vs peer-sync (code has a race; spec defines root-first precedence)
- (decision) AD candidate [ADOPTED by user]: blockchain core (src/main/blockchain + util/crypto) is a headless library, decoupled from Electron, the app shell, P2P, and HTTP; it never imports them. Binds: core units. Prevents: core drifting into app coupling, making it untestable/unreusable.
- (decision) AD candidate [ADOPTED by user]: symmetric network - all nodes are equal; no privileged/root authority node. Consensus rule: longer valid chain wins (already implemented in Blockchain.replaceChain). Binds: p2p, blockchain-core, api. Prevents: reintroducing a root-authority sync path.
- (decision) AD candidate [ADOPTED by user]: equal mining eligibility - any running node may mine; no privilege purchasable via hardware or staked balance; block reward goes to the miner of the winning block. Binds: transaction-miner, transaction-system, configuration. Prevents: stake-weighted or hardware-weighted mining re-entering the design.
- (question) OPEN: Sybil resistance - multi-wallet cheating is a stated goal but NO mechanism is settled. Candidates: off-chain proof-of-personhood, economic bond/slashing (conflicts with no-stake-privilege), or accept-and-cap. Boundary invariant is settled (no purchasable privilege); mechanism is deferred. Need user input on which candidate family to keep open.
- (event) CONFLICT FOUND: user's all-nodes-equal contradicts (a) code: src/config.js ROOT_NODE_ADDRESS = localhost:port (root is self in dev), api.js syncWithRootState() fetches root /api/blocks + pool and calls replaceChain/setMap at startup, and (b) spec: p2p-networking 'Chain-sync precedence' ADDED in reconcile-spec-conflicts (archived 2026-09-19) mandates root-first init on empty chain. Must be resolved: keep root as dev-only bootstrap, or delete the concept (pure peer sync; spec needs a follow-up change).
