schema: spec-driven

context: |
  Project: SuccinctCoin - a minimalistic cryptocurrency built as an Electron desktop app.
  
  Tech stack:
    - Electron 36 + Electron Forge 7.x for desktop packaging
    - React 19 with hooks for renderer UI
    - Express.js API server in main process (port 3001 dev / 3333 prod)
    - Webpack 12 (separate main/renderer configs)
    - Jest 30 for testing (node environment)
    - ESLint 9 with flat config
    - libp2p 3.x with TCP, FloodSub pubsub, mDNS peer discovery
    - dayjs for date/time handling (moment.js removed)
    - Node.js crypto (ECDSA prime256v1, SHA512)
    - big.js for arbitrary-precision arithmetic
    - obj2fs-hoc for persistence to ~/.succinctcoin/
    - contextIsolation: true for security
  
  Architecture:
    - src/main/ — Electron main process (blockchain, wallet, networking, API)
    - src/main/blockchain/ — Core domain: Block, Transaction, Wallet, Account, Transacti    - src/main/blockchain/ — Core domain: Block, Transaction, Wallet, Account, Tr
    - src/main/util/ — Crypto utilities
    - src/renderer/ — React UI with React Router + React Bootstrap
    - src/r nd    - srponent    - src/r nd    - srponent    - src/r nd    - srponent     
  Conventions:
    - ES6 modules (import/    - ES6 modules (import/    - ES6 modules (import/    - ES6 modules ct    - ES6 modules (import/    - ES6 modulese names    - ES6 modules (import/    - ES6 modulocat    - ES6 modules (ist.js)
    -    -    -    -    -    -    -    -    -    -    -    -    -Doc     -    -    -    -    -    -    -    -    -    - text    -    -    -    -    -    -    -    -    - n
    -    -    -    -    -    -    -    -    Tooling (Electron 28, ESLint flat config, webpack updates)
    - Phase 2: Renderer Modernization (dayjs, React compone    - Phase 2: Renderer Modernization (dayjs, React compone    - Phase 2: Rendse 4: React 19 & Security (contextIsolation: true)
    - Phase 5: PropTypes Migration (JSDoc annotations)
    - Phase 6: Electron 36 Upgrade (latest LTS)

rules:
  proposal:
    - Keep proposals focused on one capabili    - Keep proposals focused on one capabili    - Keep proposals foai    - Keep proposals focused on one capabili    - Keep proposals focused on one capabilire    - Kee MUST     - Keep proposals focused on one capabili    - Keep proposals focused on one capabili    - Keep proposals foai    - Keep proposals focused on one capabili    - Keep proposals focused on one capabilire    - Kee MUSting    - Keep proposals focused on one capabili    - Keep proposals focused on one capabili    - Keep proposals foai    - Keep proposals focused on one capabili    - Keep pras    - Keep proposals focused on one capabili    - Keep proposals focused on one capabili    - Keepon ta    - Keep proposals focused on one capabili    - Keep proposals focused on one capabili    - Keep proposals foai    - Keep proposals focused on one capabili    - Keep proposals focuor ~)     - Keep proposals focused on one capabili    - Ker f    - Keep proposals focused on one capabili  es and components.
  dependencies:
    - Do not add any new dependencies to the project unless the user explicitly requests or approves it. Solve problems using existing dependencies and built-in APIs first.
  language:
    - ALWAYS stay in JavaScript — never suggest or migrate to TypeScript
    - All new code MUST be written in JavaScript (ES6+)
    - No .ts or .tsx files should be created
    - JSDoc annotations are preferred over TypeScript for type hints
